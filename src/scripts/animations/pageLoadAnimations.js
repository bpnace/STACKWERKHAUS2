import { gsap } from 'gsap';
 
export function initPageLoadAnimation() {
  // Page Load Fade-In - This is now handled in main.js to prevent FOUC
  // gsap.from('body', { opacity: 0, duration: 1, ease: 'power2.inOut' });

  // Add other page load specific animations here if needed for other elements
  // For example, animating a logo or a specific loader icon.
} 